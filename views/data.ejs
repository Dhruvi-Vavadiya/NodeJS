<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
  <h1>Hello EJS</h1>
<p><%= var1 + " " + num1 %></p>

<ul>
  <% for(let i = 0; i < a1.length; i++) { %>
    <li><%= a1[i] %></li>
  <% } %>

  <% studens.forEach((obj) => { %>
    <li><%= obj.rno %> - <%= obj.name %></li>
  <% }) %>
</ul>

</body>
</html>